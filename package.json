{
  "name": "kaladont",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "predev": "pnpm --filter zajednicko build",
    "dev": "pnpm --parallel --filter web --filter posluzitelj dev",
    "build": "pnpm --filter zajednicko build && pnpm --filter web build && pnpm --filter posluzitelj build",
    "test": "pnpm --filter zajednicko build && pnpm --recursive test",
    "test:e2e": "playwright test",
    "test:e2e:http": "playwright test --config=playwright.http.config.ts",
    "test:e2e:install": "playwright install chromium",
    "avatar:inventory": "node skripte/avatari/provjeri-inventory.mjs",
    "avatar:contact-sheet": "node skripte/avatari/generiraj-kontaktnu-tablu.mjs",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.15.0"
  }
}
