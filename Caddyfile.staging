{
    email {$CADDY_EMAIL}
}

staging.kaladont.hr {
    encode gzip
    header X-Robots-Tag "noindex, nofollow"

    @zdravlje path /zdravlje
    handle @zdravlje {
        reverse_proxy aplikacija:3000
    }

    handle {
        reverse_proxy aplikacija:3000
    }
}
