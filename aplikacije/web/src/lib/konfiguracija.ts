/** Adresa poslužitelja (HTTP + Socket.IO) - nikad tvrdo kodirano (postavljanje-okoline.md). */
export const ADRESA_POSLUZITELJA: string = import.meta.env.VITE_ADRESA_POSLUZITELJA ?? 'http://localhost:3000';
